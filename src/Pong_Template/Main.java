package Pong_Template;

import java.awt.event.KeyListener;

import javax.swing.JFrame;
import javax.swing.JPanel;

public class Main {

	// Step 1: Create instance variables of a JFrame, JPanel, and a width and height
	// Make sure not to instantiate them yet - we will do that in the constructor
	
	
	
	
	
	public Main() { //This is the constructor
		//Step 2: Instantiate the JFrame
		
		
		//Step 3: Instantiate the JPanel as a GamePanel
		//Remember that the GamePanel extends the JPanel which allows you to do this
		
		
		//Step 4: Add the GamePanel to the JFrame using the JFrame's "add" method
		
		
		//Step 5: Add the KeyListener from the GamePanel to the JPanel
		//This uses the Jframe's "addKeyListener" method
		//Remember to typecast the GamePanel to a KeyListener when adding it to the frame
		
		
	}
	
	//Step 6: Make a private method called "setup" with a void return type
	
	
	//Step 7: Uncomment the following code and put it in the method
	//This code will create the JFrame by setting its size, location, and visibility
	
		/*frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.setSize(width, height);
		frame.setLocation(0,0);
		frame.setVisible(true);*/
	
	
	public static void main(String[] args) { //This is the main method that starts the game
		//Step 8: Create and instantiate an instance variable of this class, "Main"
		
		
		//Step 9: call the setup method from the instance variable you just created
		 
		
	}
	
	//Congratulations! You are all done with the Main class
}
